language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t pedrojsevilla/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pedrojsevilla/docker-react npm run test -- --coverage